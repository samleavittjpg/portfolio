# Production (Vercel): public URL of your Render API — no trailing slash
# Example: https://portfolio-api-xxxx.onrender.com
# Leave unset locally; Vite proxies /api and /uploads to the dev server.
# VITE_API_BASE_URL=
